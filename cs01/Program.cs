
namespace CS01
{
    class Program
    {
        static void Main(string[] args)
        {
            Exam.RunLogic();
           
            // List<int> numbers = new List<int> { 5, 12, 6, 7, 9, 11 };
            // Func<int, bool> isEven = n =>n %2 ==0;
            // Func<int, int> doubleIt = n =>n*2;
            // Func<int, string> toString = n=> $"Number: {n}";
            // foreach(var number in numbers){
            //     int numberTemp = doubleIt(number);
            //     if(isEven(number)){
            //         Console.WriteLine(toString(numberTemp));
            //     }
                
            // }     
        }
    }
}
